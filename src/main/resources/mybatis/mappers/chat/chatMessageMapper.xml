<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sanaiclub.chat.dao.ChatMessageMapper">
    <resultMap id="messageMap"
               type="com.sanaiclub.chat.model.dto.ChatMessageDTO">

        <id column="message_id" property="message_id"/>
        <result column="room_id" property="room_id"/>
        <result column="sender_id" property="sender_id"/>
        <result column="content" property="content"/>
        <result column="created_at" property="created_at"/>
        <result column="is_read" property="is_read"/>
        <result column="is_deleted" property="is_deleted"/>
        <result column="file_name" property="file_name"/>
        <result column="file_url" property="file_url"/>
        <result column="file_size" property="file_size"/>

    </resultMap>

    <select id="findMessages"
            parameterType="int"
            resultMap="messageMap">
        SELECT
            *
        FROM messages m
        WHERE m.room_id = #{room_id}
        ORDER BY m.created_at ASC

    </select>

    <insert id="insertMessage">
        INSERT INTO messages (
            room_id,
            sender_id,
            content,
            file_name,
            file_url,
            file_size,
            created_at,
            is_read
        ) VALUES (
                     #{room_id},
                     #{sender_id},
                     #{content},
                     #{file_name},
                     #{file_url},
                     #{file_size},
                    CURRENT_TIMESTAMP,
                  0
                 )
    </insert>

    <update id="markRoomMessagesAsRead">
        UPDATE messages
        SET is_read = 1
        WHERE room_id = #{room_id}
          AND sender_id != #{login_user_id}
          AND is_read = 0
    </update>


    <update id="deleteMessage">
        UPDATE messages
        SET is_deleted = 1, deleted_at = CURRENT_TIMESTAMP
        WHERE message_id = #{message_id}
    </update>


</mapper>
