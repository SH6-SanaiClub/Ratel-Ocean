<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sanaiclub.user.dao.UserMapper">

    <insert id="insertUser" parameterType="com.sanaiclub.user.model.vo.UserVO"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (
            login_id, email, password, user_type, name, phone, birth_date, status
        ) VALUES (
                     #{loginId}, #{email}, #{password}, #{userType}, #{name}, #{phone}, #{birthDate}, 'ACTIVE'
                 )
    </insert>

    <select id="checkId" resultType="int">
        SELECT COUNT(*) FROM users WHERE login_id = #{loginId}
    </select>

    <select id="checkEmail" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

</mapper>