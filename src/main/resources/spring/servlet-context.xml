<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:jwt.properties, classpath:deepseek-api.properties" ignore-unresolvable="true"/>

    <!-- Annotation-driven MVC -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <!-- String Converter (UTF-8) -->
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>text/html;charset=UTF-8</value>
                        <value>text/plain;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
            <!-- JSON Converter -->
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 정적 리소스 매핑 -->
    <mvc:resources mapping="/resources/**" location="/resources/"/>
    <mvc:resources mapping="/css/**" location="/resources/css/"/>
    <mvc:resources mapping="/js/**" location="/resources/js/"/>
    <!-- images 폴더가 실제로 없다면 아래 라인은 주석 처리 또는 삭제해도 무방 -->
    <!-- <mvc:resources mapping="/images/**" location="/resources/images/"/> -->

    <!-- Interceptor 등록 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 모든 URL에 적용 -->
            <mvc:mapping path="/**"/>

            <!-- 제외할 URL (비로그인 허용) -->
            <mvc:exclude-mapping path="/"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/join/**"/>
            <mvc:exclude-mapping path="/test"/>
            <mvc:exclude-mapping path="/api/test"/>
            <mvc:exclude-mapping path="/db/test"/>
            <mvc:exclude-mapping path="/resources/**"/>
            <mvc:exclude-mapping path="/css/**"/>
            <mvc:exclude-mapping path="/js/**"/>
            <mvc:exclude-mapping path="/images/**"/>
            <!-- contract 관련 URL 인증 예외 추가 -->
            <mvc:exclude-mapping path="/contract/**"/>

            <!-- 인증 인터셉터 -->
            <bean class="com.sanaiclub.common.interceptor.AuthenticationInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- ViewResolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
        <property name="contentType" value="text/html;charset=UTF-8"/>
    </bean>

    <!-- Controller 패키지만 스캔 -->
    <context:component-scan base-package="com.sanaiclub.**.controller"/>

    <!-- 파일 업로드 설정 -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="52428800"/> <!-- 50MB -->
        <property name="maxInMemorySize" value="1048576"/> <!-- 1MB -->
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

</beans>